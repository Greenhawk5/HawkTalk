// Phase 2 transport acknowledgement, extracted for the Phase 6 transport-only
// fallback mode (no conversational flow supplied). It proves transport
// end-to-end without faking an AI response.
export const TRANSPORT_ACK_TEXT =
  '👋 HawkTalk here — I got your message! The chat core isn’t connected yet, so I can’t reply properly. I’ll be back with you shortly 🙂';
